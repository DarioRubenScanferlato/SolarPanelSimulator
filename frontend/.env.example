# Frontend Environment Configuration Template
# For vanilla JavaScript (no build tool), API URL is configured via window.APP_API_URL
# Set in index.html: <script>window.APP_API_URL = "https://api.example.com";</script>
# Default fallback: http://localhost:8000

API_URL=http://localhost:8000
